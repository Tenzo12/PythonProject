import random
import string

def generate(length):

    characterrs = string.digits + string.ascci_letters + string.punctuation

    password = "" + random.choice(characters) for x in range(length)

    return password


print(generate(7))



list_of_numbers = []
list_of_numbers.add(


A program that checks that given year is aleap year

